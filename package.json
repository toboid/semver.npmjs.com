{
  "name": "semver.npmjs.com",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "build-js": "browserify src/entry.js -o build/bundle.js",
    "build-css": "stylus --compress < src/styles.styl > build/styles.css && cp node_modules/angular/angular-csp.css build/",
    "build": "npm run build-js && npm run build-css && cp src/index.html build/index.html",
    "livereload": "live-reload --port 9091 ./build/*",
    "watch": "watch 'npm run build' src/"
  },
  "dependencies": {
    "angular": "^1.3.15",
    "semver": "^4.3.1"
  },
  "devDependencies": {
    "browserify": "^9.0.3",
    "live-reload": "^1.1.0",
    "stylus": "^0.50.0"
  }
}
